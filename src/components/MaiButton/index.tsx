interface Props {
  text: string;
  position: number;
  type: string;
  onClick: () => void;
}

// eslint-disable-next-line import/no-anonymous-default-export
export default () => {
  return <></>;
};
